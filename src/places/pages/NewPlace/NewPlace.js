import React from 'react';

import './NewPlace.css';

const NewPlace = () => {
    return (
        <form className="place-form">
            {/* <Input
                element="input"
                id="new-place-title"
                type="text"
                label="Title"
                validators={[]}
                errorText="Title is required"
            /> */}
        </form>
    );
};

export default NewPlace;